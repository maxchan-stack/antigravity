import AppLayout from './AppLayout';
import './styles/global.css'; // Keep global styles but override with figma-theme inside layout if needed

export default function App() {
    return (
        <AppLayout />
    );
}
